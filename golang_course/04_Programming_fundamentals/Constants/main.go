package main

import (
	"fmt"
)

const (
	no = 40
	name = "Maaz"
	fb =10.0
)
func main()  {

	fmt.Print(no)
	fmt.Printf("\t%T\n", no)

	fmt.Print(name)
	fmt.Printf("\t%T\n", name)

	fmt.Print(fb)
	fmt.Printf("\t%T\n", fb)
}
